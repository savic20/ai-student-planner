{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c 'from app.db.database import engine, Base; from app.db.models import *; Base.metadata.create_all(bind=engine)' && python -m app.main",
    "restartPolicyType": "ON_FAILURE"
  }
}
